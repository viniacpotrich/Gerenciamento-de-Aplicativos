import 'package:flutter/material.dart';

class ScreenErrorPage extends StatelessWidget {
  const ScreenErrorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Expanded(child: Column()));
  }
}
