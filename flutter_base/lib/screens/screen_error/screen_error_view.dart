import 'package:flutter/material.dart';

class ScreenErrorView extends StatelessWidget {
  const ScreenErrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Expanded(child: Column()));
  }
}
